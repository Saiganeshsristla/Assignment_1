What is SPA?
-> SPA (Single page application) is a implementation that loads the Single web page on the browser and then update that web page later.
This therefore allows users to use websites without loading whole new pages from the server